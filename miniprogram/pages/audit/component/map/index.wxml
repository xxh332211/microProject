<!--pages/audit/component/map/index.wxml-->
<view class="map">
  <map longitude="{{center.lng}}" latitude="{{center.lat}}" scale="12.5" polygons="{{polygons}}" polyline="{{polyline}}">
  </map>
</view>
